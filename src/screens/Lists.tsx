import { View,Text } from 'react-native';
export const Lists = () => {
  return(
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Lists</Text>
    </View>
  )
}